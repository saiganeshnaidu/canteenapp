<% Notification.all.reverse.each do |x|  %>
  <% if x.recipient_type=="User" %>
    <%= link_to User.find(x.params[:cart][:user_id]).email+" has ordered food on "+x.params[:cart].food_store.name, edit_cart_path(x.params[:cart][:id])  %><br><br>
  <% end %>
<% end %>

